
# MERF App (Monitoring and Evaluation Request Form)

## How to run

1. Install dependencies:
   pip install -r requirements.txt

2. Run the app:
   streamlit run app.py

## Notes
- Uploaded files are saved in the 'uploads' folder
- Data is saved in 'merf_data.csv'
